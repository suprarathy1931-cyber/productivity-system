{
  "name": "productivity-system-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute productivity-db --local --file=../schema/schema.sql",
    "db:migrate:remote": "wrangler d1 execute productivity-db --remote --file=../schema/schema.sql",
    "db:seed:local": "wrangler d1 execute productivity-db --local --file=../schema/seed.sql",
    "db:seed:remote": "wrangler d1 execute productivity-db --remote --file=../schema/seed.sql"
  },
  "dependencies": {
    "itty-router": "^5.0.18"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
